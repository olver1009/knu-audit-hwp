# 작성 규칙

- 출력 단위: 같은 `transaction_id`·결제일자·관·항·영수증 코드끼리 합산한다.
- 다른 목: 영수증이 같아도 별도 HWP로 만든다.
- 영수증 코드: `<도26-{학기}{재원}{목 3자리}>` 형식이다.
  - 학기: 상반기 `1`, 하반기 `2`
  - 재원: 총학생회비·운영비 `1`, 자체수익금 `4`
  - 철거비는 자체수익금으로 본다.
- 목적: 간결한 공문체로 쓰고 원칙적으로 `~하기 위함.`으로 끝낸다.
- 비고: 별도 사유가 없으면 `{구매처}에서 구매함.`으로 쓴다.
- 금액: 천 단위 쉼표와 `원`을 붙인다.
- 날짜: `YYYY.MM.DD` 형식을 쓴다. 작성일자는 마침표까지 포함한 `YYYY.MM.DD.` 형식이다.
- 질문: 결제일자·금액·관·항·목 코드처럼 문서를 작성할 수 없게 하는 값만 질문한다. 증빙 제출 관련 안내는 하지 않는다.
